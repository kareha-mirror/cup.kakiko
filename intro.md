# kakiko - SKKの方式で端末に日本語を入力するツール(SKK-FEPの一種)

## これは何？

**kakiko** は端末で日本語を入力するツールであり、フロントエンドプロセッサ(**FEP**)と呼ばれるものの一種である。

端末は文字で対話的にコマンドを入力したり文字主体のツールを使うためのインターフェイスであり、LinuxやBSDなどのUnix系OSの基本機能である。
現代の一般的な日本語の入力系はデスクトップ環境に依存しているが、kakikoはそれらに依存せず端末の機能だけで動くFEPである。
そのためSSHなどを使い遠隔のサーバ上で作業する場合などに、手元のデスクトップ環境に依らずに使えるなどの利点がある。
現代ではLinuxではX Window SystemからWaylandへの移行期であると共に、BSDでは依然としてX Window Systemが使われ続ける見込みであることもあり、環境に依らず端末の機能だけで使えるツールには価値がある。

kakikoでの日本語の入力には **SKK** の方式を使う。
SKKの最大の特徴は文法的な知識を使わずに仮名を漢字に変換することである。
そのため意図しない誤変換に悩まされることが無い。
SKKは日本語入力系の中ではそれほど一般的ではないが、ウェブ上で解説が見付かるはずなので、詳細は他の文書に譲る。
kakikoは出来るだけ元祖のSKKに近い動作を再現するよう心掛けている。
とはいえ、元祖のSKKはEmacsというテキストエディタで動くものである一方、kakikoは端末で汎用的に使うものであるので、元祖のSKKと動作が異なる部分がいくらかある。
それでも日本語を入力する基本的な部分は、他の環境でSKKに親しんでいれば、kakikoにも馴染めるはずである。

## 起動のしかた

kakikoを使うには端末で `kakiko` コマンドを実行する。
すると新しいシェルが起動し、端末の画面の最下行にkakikoが確保したステイタス行が表示される。
起動直後はステイタス行に `(SKK:)` と表示されASCII入力モードであることが示されている。
ここでキーボードから入力すれば、通常通りにASCII文字を入力することが出来る。
一方、日本語を入力するには `Ctrl-J` を押して平仮名入力モードに切り替える。
するとステイタス行には `(かな)` と表示され、通常のSKKの方式で平仮名を入力し、変換して漢字も入力できる。
平仮名モードからASCIIモードに戻るには `l` (小文字のエル) を押す。

kakikoはFEPとして動作する。
つまり、端末への入力の前段に構えて入力を変換し、その結果を端末内のアプリケーションに渡す。
そのためkakikoを起動した後に立ち上がるシェルの中で更に起動したアプリケーション、例えばテキストエディタなどにも、kakikoで日本語を入力することが出来る。

## 辞書

### 組み込み辞書

kakikoにはあらかじめ最低限の辞書が組み込まれている。
この辞書は常用漢字表から起こしたものと、自動生成された文書を手入力(比喩的に「写経」と呼んでいる)することで育てた辞書を、組み合わせたものである。
合わせてせいぜい1万行程度なので、実用には足りないが、SKKの本来の精神で自分で辞書を育てる基礎としては十分と考えられる。

### ユーザ辞書

kakikoを使って辞書を登録した場合、kakikoの終了時に設定ディレクトリ内に新しい変換規則が保存される。
設定ディレクトリは通常、ホームディレクトリの下の `.config/kakiko/` になっている。
新しく登録した変換語句は `skk-edic-user.txt` と `skk-edic-diff.txt` に保存される。

複数の端末でそれぞれkakikoを使う場合、それぞれのユーザ辞書の記憶は自動では同期されない。
手動で同期するには、同期したい端末のkakikoで `Ctrl-L Ctrl-G` を押す。

### ラージ辞書

組み込みの辞書と自分で育てるユーザ辞書以外に、元祖のSKKのラージ辞書(`SKK-JISYO.L`)をkakikoで使えるよう変換したものも用意してある。
こちらはライセンスがGPLなので、kakiko本体に組み込むことが出来無いため、別途ダウンロードして配置する。
簡単のため `kakiko -download` コマンドを用意してある。
これを実行するとラージ辞書がkakikoの設定ディレクトリに自動的に配置され、次回のkakikoの起動時から使えるようになる。
なお、保存される辞書のファイルは `skk-edic-legacy-large.cdb` という名前で、検索に効率が良いCDBという形式であり、テキストとしては読めない。

## その他の機能

### vi系のテキストエディタ向けの機能

vi系のテキストエディタでは、Escを押すと挿入モードからコマンドモードに戻るが、その際に仮名モードなどになっていると煩わしい。
なのでkakikoではEscを押すとアプリケーションにEscを送ると共に、ASCIIモードに戻るようにしてある。

なお、端末の仕様上、キーボードからのEscキーの入力を単独で判定するのは難しい。
kakikoではEsc文字をバッファし次の文字が来てから単独のEscキーの入力かどうかを判定するため、Escキーを押してもすぐにはアプリケーションに送られない。
通常は気にせずEscキーに続いて他のキーを押すことで意図通りに動作するはずである。
念のためEscキーが入力中であることを確認できるようステイタス行の右端に示した。
通常は「.」と表示されているものが、Escキーの入力中にはアプリケーションに送られる前に「*」と表示される。

### 特殊なキーの透過

kakikoでは `Ctrl-J`, `Ctrl-G`, `Ctrl-L` を特殊な用途に使っているので、そのままではこれらの特殊文字をアプリケーションに送ることが出来無い。
送るには、それぞれ特別な操作や条件がある。

* `Ctrl-J` をアプリケーションに送るには、平仮名または片仮名モードで、他の文字の入力途中ではない状態で `Ctrl-J` を押す。
* `Ctrl-G` をアプリケーションに送るには、各モードでキャンセルの対象になる入力途中ではない状態で `Ctrl-G` を押す。
* `Ctrl-L` をアプリケーションに送るには、行バッファ入力途中以外の状態で `Ctrl-L` を2回連続で押す。

これらの操作は、良く分からなくても連打すれば通るようになっている。

### 行バッファモード

通常kakikoは変換が確定されるたびに端末内のアプリケーションに変換された文字列を入力する。
しかしたまにこれでは困る場合がある。
アプリケーション側が全角文字の処理に完全に対応していない場合、入力した全角文字をバックスペースで削除するなど編集しようとした際に、文字が崩れたりカーソルの位置が不正になったりすることがある。
それに対処するため、kakikoには行バッファモードを追加してある。
`Ctrl-L` を押すと行バッファモードに移行する。
このモードでは、入力した文字の変換が確定してもステイタス行に表示されるだけでアプリケーションに送信されず、長い文字列を事前に作ることが出来る。
最後に `Enter` を追加で押すと、貯めた文字列を一度にアプリケーションに入力することが出来る。
行バッファモードから通常の逐次入力モードに戻るには、もう一度 `Ctrl-L`を押す。

### 設定ディレクトリの指定

kakiko起動時に `-d` オプションで、使う設定ディレクトリを指定できる。

### 最小辞書モード

kakiko起動時に `-joyo` オプションを指定すると、組み込み辞書に常用漢字表の辞書だけを使い、「写経」辞書を読み込まない。
これと `-d` オプションを組み合わせれば、最小辞書モードを実現できる。

### ユーザ辞書のアンロック

ユーザ辞書に書き込む際に複数のkakikoプロセスが競合しないよう、書き込み作業中は設定ディレクトリ内に一時的に排他的ロックを作る。
システムがクラッシュしたときなど、このロックが残ってしまい、kakikoの動作に支障が出ることがある。
その場合 `-unlock` オプションを指定することで、ロックを強制的に解除することで復帰できる。

## ステイタス行の色の指定

[color.md](color.md) を参照。
